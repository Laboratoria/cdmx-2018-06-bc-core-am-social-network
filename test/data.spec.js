// Ejecuta tu test 
let assert  = require('assert');
let expecting = require('chai'), expect,
let deberia = require('chai'), shoild, 

describe('config', function () => {
 ('deberia ser una funcion con la configuración de firebase')
})

describe('mandarUsuarioDatabase', function () => {

  it('deberia regresar username', () => {
     assert.equal(mandarUsuarioDatabase, 'username');
  });
  it('deberia regresar email', () => {
     assert.equal(mandarUsrarioDatabase, 'email');
  });
};

describe(deleteMsg = () => {

  it('Deberia lanzar un alert si quiere el usuario borrar su post', function () => {
    let confirmar = if (confirm('¿Quieres eliminar este mensaje?') === true)
    assert.equal(confirm, true);
  });
}); 
